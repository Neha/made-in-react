import React from 'react';
import { render, screen } from '@testing-library/react';

describe("<Label/> component", () => {
    test.todo("Should render");
    test.todo("Should have label");
    test.todo("Should have for");
    test.todo("Should have id");
})